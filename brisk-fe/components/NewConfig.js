// Config starter code
import { createChatBotMessage, createClientMessage } from "react-chatbot-kit";


console.log("new config.js is running right now");
const config = {
  initialMessages: [createClientMessage(`Hi, I'm here to help. Let me try whatever to help speed up whatever you need to get done.`)]
}

export default config;